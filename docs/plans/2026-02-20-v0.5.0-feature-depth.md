# v0.5.0 Feature Depth Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add configurable confidence tuning, memory export/import, auto-consolidation scheduling, and adaptive consolidation to Audrey.

**Architecture:** Bottom-up approach. First thread existing confidence config params through the call chain (the formula already accepts them). Then add JSON export/import for backup/migration. Then add interval-based auto-consolidation. Finally, adaptive consolidation that learns optimal cluster sizes per domain. Each feature builds on the last — config threading enables tuning, export preserves tuned state, auto-consolidation uses the config, adaptive consolidation adjusts it.

**Tech Stack:** Node.js ES modules, SQLite/better-sqlite3, sqlite-vec, vitest

---

### Task 1: Thread custom weights through recall

The `computeConfidence()` function already accepts optional `weights` and `customSourceReliability` params, but the 3 confidence helpers in `recall.js` hardcode `DEFAULT_HALF_LIVES` and pass no custom weights. Thread an optional `confidenceConfig` through the recall pipeline.

**Files:**
- Modify: `src/recall.js:1-47` (confidence helper functions + import)
- Modify: `src/recall.js:115-191` (knn functions that call helpers)
- Modify: `src/recall.js:200-274` (recallStream/recall that accept options)
- Test: `tests/recall.test.js`

**Step 1: Write the failing test**

Add to `tests/recall.test.js`:

```js
it('respects custom halfLives passed via confidenceConfig', async () => {
  // With a very short episodic half-life (0.01 days), even fresh memories decay fast
  // Default episodic half-life is 7 days, so at 0 days age, recency=1.0 for both
  // But this test encodes memories, then recalls with a half-life so short that
  // the recency component is nearly zero, lowering overall confidence
  const shortHalfLife = await recall(db, embedding, 'rate limit', {
    confidenceConfig: {
      halfLives: { episodic: 0.001, semantic: 0.001, procedural: 0.001 },
    },
    types: ['episodic'],
  });
  const normalHalfLife = await recall(db, embedding, 'rate limit', {
    types: ['episodic'],
  });
  // Short half-life memories should have lower confidence than default
  // Both queries return same memories, but confidence scores differ
  if (shortHalfLife.length > 0 && normalHalfLife.length > 0) {
    expect(shortHalfLife[0].confidence).toBeLessThan(normalHalfLife[0].confidence);
  }
});

it('respects custom weights passed via confidenceConfig', async () => {
  // Source-only weighting: all weight on source reliability
  const sourceOnly = await recall(db, embedding, 'rate limit', {
    confidenceConfig: {
      weights: { source: 1.0, evidence: 0, recency: 0, retrieval: 0 },
    },
    types: ['episodic'],
  });
  // For direct-observation, source reliability = 0.95
  // With weights {source:1, rest:0}, confidence should be exactly 0.95
  if (sourceOnly.length > 0) {
    expect(sourceOnly[0].confidence).toBeCloseTo(0.95, 1);
  }
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/recall.test.js`
Expected: FAIL — `confidenceConfig` is not recognized, default behavior for both calls

**Step 3: Write minimal implementation**

In `src/recall.js`, change the 3 compute helpers to accept a `confidenceConfig` param:

```js
function computeEpisodicConfidence(ep, now, confidenceConfig = {}) {
  const ageDays = daysBetween(ep.created_at, now);
  const halfLives = confidenceConfig.halfLives || DEFAULT_HALF_LIVES;
  return computeConfidence({
    sourceType: ep.source,
    supportingCount: 1,
    contradictingCount: 0,
    ageDays,
    halfLifeDays: halfLives.episodic ?? DEFAULT_HALF_LIVES.episodic,
    retrievalCount: 0,
    daysSinceRetrieval: ageDays,
    weights: confidenceConfig.weights,
    customSourceReliability: confidenceConfig.sourceReliability,
  });
}
```

Apply the same pattern to `computeSemanticConfidence` and `computeProceduralConfidence` (using `halfLives.semantic` and `halfLives.procedural` respectively).

Thread `confidenceConfig` through:
- `knnEpisodic(db, queryBuffer, candidateK, now, minConfidence, includeProvenance, confidenceConfig)`
- `knnSemantic(db, queryBuffer, candidateK, now, minConfidence, includeProvenance, includeDormant, confidenceConfig)`
- `knnProcedural(db, queryBuffer, candidateK, now, minConfidence, includeProvenance, includeDormant, confidenceConfig)`

Extract `confidenceConfig` from `options` in `recallStream`:
```js
const {
  minConfidence = 0,
  types,
  limit = 10,
  includeProvenance = false,
  includeDormant = false,
  confidenceConfig,
} = options;
```

Pass it through to each knn call.

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/recall.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/recall.js tests/recall.test.js
git commit -m "feat: thread confidenceConfig through recall pipeline"
```

---

### Task 2: Thread custom halfLives through decay

`applyDecay()` in `src/decay.js` hardcodes `DEFAULT_HALF_LIVES.semantic` and `DEFAULT_HALF_LIVES.procedural`. Accept optional `halfLives` in the options.

**Files:**
- Modify: `src/decay.js:9` (function signature)
- Modify: `src/decay.js:29-37` (semantic confidence call)
- Modify: `src/decay.js:60-68` (procedural confidence call)
- Test: `tests/decay.test.js`

**Step 1: Write the failing test**

Add to `tests/decay.test.js`:

```js
it('respects custom halfLives for semantic decay', () => {
  const id = generateId();
  db.prepare(`
    INSERT INTO semantics (id, content, state, supporting_count, contradicting_count,
      retrieval_count, created_at)
    VALUES (?, ?, ?, ?, ?, ?, ?)
  `).run(id, 'Test half-life fact', 'active', 3, 0, 0, daysAgo(10));

  // With default semantic half-life of 30 days, 10-day-old memory stays active
  const defaultResult = applyDecay(db, { dormantThreshold: 0.5 });
  const afterDefault = db.prepare('SELECT state FROM semantics WHERE id = ?').get(id);
  expect(afterDefault.state).toBe('active');

  // Reset state
  db.prepare("UPDATE semantics SET state = 'active' WHERE id = ?").run(id);

  // With 1-day semantic half-life, 10-day-old memory (~10 half-lives) goes dormant
  const customResult = applyDecay(db, {
    dormantThreshold: 0.5,
    halfLives: { semantic: 1, procedural: 90 },
  });
  const afterCustom = db.prepare('SELECT state FROM semantics WHERE id = ?').get(id);
  expect(afterCustom.state).toBe('dormant');
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/decay.test.js`
Expected: FAIL — `halfLives` option not recognized

**Step 3: Write minimal implementation**

In `src/decay.js`, modify the function signature and usage:

```js
export function applyDecay(db, { dormantThreshold = 0.1, halfLives } = {}) {
```

Replace `DEFAULT_HALF_LIVES.semantic` with:
```js
halfLifeDays: halfLives?.semantic ?? DEFAULT_HALF_LIVES.semantic,
```

Replace `DEFAULT_HALF_LIVES.procedural` with:
```js
halfLifeDays: halfLives?.procedural ?? DEFAULT_HALF_LIVES.procedural,
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/decay.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/decay.js tests/decay.test.js
git commit -m "feat: thread custom halfLives through decay"
```

---

### Task 3: Wire confidence config from Audrey constructor

Connect the new `confidenceConfig` and `halfLives` options through the Audrey class so users configure once at construction time.

**Files:**
- Modify: `src/audrey.js:75-102` (constructor)
- Modify: `src/audrey.js:163-164` (recall method)
- Modify: `src/audrey.js:172-173` (recallStream method)
- Modify: `src/audrey.js:197-202` (decay method)
- Test: `tests/audrey.test.js`

**Step 1: Write the failing test**

Add to `tests/audrey.test.js`:

```js
describe('confidence config', () => {
  let audrey;
  const CONF_DIR = './test-confidence-config';

  beforeEach(() => {
    if (existsSync(CONF_DIR)) rmSync(CONF_DIR, { recursive: true });
  });

  afterEach(() => {
    audrey?.close();
    if (existsSync(CONF_DIR)) rmSync(CONF_DIR, { recursive: true });
  });

  it('passes custom weights through to recall', async () => {
    audrey = new Audrey({
      dataDir: CONF_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
      confidence: {
        weights: { source: 1.0, evidence: 0, recency: 0, retrieval: 0 },
      },
    });
    await audrey.encode({ content: 'test memory', source: 'direct-observation' });
    const results = await audrey.recall('test', { types: ['episodic'] });
    // source-only weight on direct-observation = 0.95
    expect(results.length).toBeGreaterThan(0);
    expect(results[0].confidence).toBeCloseTo(0.95, 1);
  });

  it('passes custom halfLives through to decay', async () => {
    audrey = new Audrey({
      dataDir: CONF_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
      confidence: {
        halfLives: { episodic: 7, semantic: 1, procedural: 90 },
      },
    });

    // Insert a 10-day-old semantic memory directly
    const now = new Date();
    const tenDaysAgo = new Date(now - 10 * 86400000).toISOString();
    audrey.db.prepare(`
      INSERT INTO semantics (id, content, state, supporting_count, contradicting_count,
        retrieval_count, created_at)
      VALUES (?, ?, ?, ?, ?, ?, ?)
    `).run('sem-hl', 'Half-life test', 'active', 3, 0, 0, tenDaysAgo);

    // With 1-day half-life, 10-day-old memory should go dormant at threshold 0.5
    const result = audrey.decay({ dormantThreshold: 0.5 });
    const row = audrey.db.prepare('SELECT state FROM semantics WHERE id = ?').get('sem-hl');
    expect(row.state).toBe('dormant');
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/audrey.test.js`
Expected: FAIL — `confidence` config option not recognized

**Step 3: Write minimal implementation**

In `src/audrey.js` constructor, add `confidence = {}` to destructured config:

```js
constructor({
  dataDir = './audrey-data',
  agent = 'default',
  embedding = { provider: 'mock', dimensions: 64 },
  llm,
  confidence = {},
  consolidation = {},
  decay = {},
} = {}) {
```

Store it:
```js
this.confidenceConfig = {
  weights: confidence.weights,
  halfLives: confidence.halfLives,
  sourceReliability: confidence.sourceReliability,
};
```

Thread it into `recall()`:
```js
recall(query, options = {}) {
  return recallFn(this.db, this.embeddingProvider, query, {
    ...options,
    confidenceConfig: options.confidenceConfig ?? this.confidenceConfig,
  });
}
```

Thread it into `recallStream()`:
```js
async *recallStream(query, options = {}) {
  yield* recallStreamFn(this.db, this.embeddingProvider, query, {
    ...options,
    confidenceConfig: options.confidenceConfig ?? this.confidenceConfig,
  });
}
```

Thread halfLives into `decay()`:
```js
decay(options = {}) {
  const result = applyDecay(this.db, {
    dormantThreshold: options.dormantThreshold || this.decayConfig.dormantThreshold,
    halfLives: options.halfLives ?? this.confidenceConfig.halfLives,
  });
  this.emit('decay', result);
  return result;
}
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/audrey.test.js`
Expected: PASS

**Step 5: Run full test suite**

Run: `npx vitest run`
Expected: All 208+ tests pass

**Step 6: Commit**

```bash
git add src/audrey.js tests/audrey.test.js
git commit -m "feat: wire confidence config from Audrey constructor to recall and decay"
```

---

### Task 4: Memory export

Add `audrey.export()` that returns a JSON-serializable snapshot of all memory state (episodes, semantics, procedures, causal links, contradictions, consolidation runs, config). No embeddings in the export (they're recomputable).

**Files:**
- Create: `src/export.js`
- Modify: `src/audrey.js` (add export method)
- Modify: `src/index.js` (re-export)
- Test: `tests/export.test.js`

**Step 1: Write the failing test**

Create `tests/export.test.js`:

```js
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { Audrey } from '../src/index.js';
import { existsSync, rmSync } from 'node:fs';

const TEST_DIR = './test-export-data';

describe('export', () => {
  let audrey;

  beforeEach(async () => {
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
    audrey = new Audrey({
      dataDir: TEST_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await audrey.encode({ content: 'Memory one', source: 'told-by-user', tags: ['a'] });
    await audrey.encode({ content: 'Memory two', source: 'direct-observation' });
  });

  afterEach(() => {
    audrey.close();
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
  });

  it('returns a snapshot with all tables', () => {
    const snapshot = audrey.export();
    expect(snapshot).toHaveProperty('version');
    expect(snapshot).toHaveProperty('exportedAt');
    expect(snapshot).toHaveProperty('episodes');
    expect(snapshot).toHaveProperty('semantics');
    expect(snapshot).toHaveProperty('procedures');
    expect(snapshot).toHaveProperty('causalLinks');
    expect(snapshot).toHaveProperty('contradictions');
    expect(snapshot).toHaveProperty('consolidationRuns');
    expect(snapshot).toHaveProperty('config');
  });

  it('includes all episodes', () => {
    const snapshot = audrey.export();
    expect(snapshot.episodes.length).toBe(2);
    expect(snapshot.episodes[0]).toHaveProperty('id');
    expect(snapshot.episodes[0]).toHaveProperty('content');
    expect(snapshot.episodes[0]).toHaveProperty('source');
  });

  it('excludes raw embedding blobs', () => {
    const snapshot = audrey.export();
    for (const ep of snapshot.episodes) {
      expect(ep).not.toHaveProperty('embedding');
    }
  });

  it('preserves tags as arrays', () => {
    const snapshot = audrey.export();
    const tagged = snapshot.episodes.find(e => e.content === 'Memory one');
    expect(tagged.tags).toEqual(['a']);
  });

  it('produces valid JSON', () => {
    const snapshot = audrey.export();
    const json = JSON.stringify(snapshot);
    const parsed = JSON.parse(json);
    expect(parsed.episodes.length).toBe(2);
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/export.test.js`
Expected: FAIL — `audrey.export is not a function`

**Step 3: Write implementation**

Create `src/export.js`:

```js
import { VERSION } from '../mcp-server/config.js';
import { safeJsonParse } from './utils.js';

export function exportMemories(db) {
  const episodes = db.prepare(
    'SELECT id, content, source, source_reliability, salience, tags, causal_trigger, causal_consequence, created_at, supersedes, superseded_by, consolidated FROM episodes'
  ).all().map(ep => ({
    ...ep,
    tags: safeJsonParse(ep.tags, null),
  }));

  const semantics = db.prepare(
    'SELECT id, content, state, conditions, evidence_episode_ids, evidence_count, supporting_count, contradicting_count, source_type_diversity, consolidation_checkpoint, created_at, last_reinforced_at, retrieval_count, challenge_count FROM semantics'
  ).all().map(sem => ({
    ...sem,
    evidence_episode_ids: safeJsonParse(sem.evidence_episode_ids, []),
  }));

  const procedures = db.prepare(
    'SELECT id, content, state, trigger_conditions, evidence_episode_ids, success_count, failure_count, created_at, last_reinforced_at, retrieval_count FROM procedures'
  ).all().map(proc => ({
    ...proc,
    evidence_episode_ids: safeJsonParse(proc.evidence_episode_ids, []),
  }));

  const causalLinks = db.prepare('SELECT * FROM causal_links').all();

  const contradictions = db.prepare(
    'SELECT id, claim_a_id, claim_a_type, claim_b_id, claim_b_type, state, resolution, resolved_at, reopened_at, reopen_evidence_id, created_at FROM contradictions'
  ).all();

  const consolidationRuns = db.prepare(
    'SELECT id, input_episode_ids, output_memory_ids, started_at, completed_at, status FROM consolidation_runs'
  ).all().map(run => ({
    ...run,
    input_episode_ids: safeJsonParse(run.input_episode_ids, []),
    output_memory_ids: safeJsonParse(run.output_memory_ids, []),
  }));

  const configRows = db.prepare('SELECT key, value FROM audrey_config').all();
  const config = Object.fromEntries(configRows.map(r => [r.key, r.value]));

  return {
    version: VERSION,
    exportedAt: new Date().toISOString(),
    episodes,
    semantics,
    procedures,
    causalLinks,
    contradictions,
    consolidationRuns,
    config,
  };
}
```

Add to `src/audrey.js`:
```js
import { exportMemories } from './export.js';
```

Add method:
```js
export() {
  return exportMemories(this.db);
}
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/export.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/export.js src/audrey.js tests/export.test.js
git commit -m "feat: add memory export"
```

---

### Task 5: Memory import

Add `audrey.import(snapshot)` that restores a previously exported snapshot into a fresh database. Re-embeds all content using the current embedding provider.

**Files:**
- Create: `src/import.js`
- Modify: `src/audrey.js` (add import method)
- Test: `tests/import.test.js`

**Step 1: Write the failing test**

Create `tests/import.test.js`:

```js
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { Audrey } from '../src/index.js';
import { existsSync, rmSync } from 'node:fs';

const EXPORT_DIR = './test-import-export';
const IMPORT_DIR = './test-import-dest';

describe('import', () => {
  let source, dest;

  beforeEach(async () => {
    if (existsSync(EXPORT_DIR)) rmSync(EXPORT_DIR, { recursive: true });
    if (existsSync(IMPORT_DIR)) rmSync(IMPORT_DIR, { recursive: true });
    source = new Audrey({
      dataDir: EXPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await source.encode({ content: 'Export test one', source: 'told-by-user', tags: ['test'] });
    await source.encode({ content: 'Export test two', source: 'direct-observation' });
  });

  afterEach(() => {
    source?.close();
    dest?.close();
    if (existsSync(EXPORT_DIR)) rmSync(EXPORT_DIR, { recursive: true });
    if (existsSync(IMPORT_DIR)) rmSync(IMPORT_DIR, { recursive: true });
  });

  it('round-trips episodes through export/import', async () => {
    const snapshot = source.export();
    dest = new Audrey({
      dataDir: IMPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await dest.import(snapshot);
    const stats = dest.introspect();
    expect(stats.episodic).toBe(2);
  });

  it('preserves episode metadata', async () => {
    const snapshot = source.export();
    dest = new Audrey({
      dataDir: IMPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await dest.import(snapshot);
    const ep = dest.db.prepare("SELECT * FROM episodes WHERE content = 'Export test one'").get();
    expect(ep.source).toBe('told-by-user');
    expect(JSON.parse(ep.tags)).toEqual(['test']);
  });

  it('re-embeds content with current provider', async () => {
    const snapshot = source.export();
    dest = new Audrey({
      dataDir: IMPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await dest.import(snapshot);
    // Verify vec_episodes has entries
    const vecCount = dest.db.prepare('SELECT COUNT(*) as c FROM vec_episodes').get().c;
    expect(vecCount).toBe(2);
  });

  it('imports into empty database only', async () => {
    const snapshot = source.export();
    // Import into source which already has data
    await expect(source.import(snapshot)).rejects.toThrow('not empty');
  });

  it('imports semantic memories', async () => {
    // Consolidate to create semantic memories
    await source.encode({ content: 'Export test one', source: 'tool-result' });
    await source.consolidate({ minClusterSize: 2, similarityThreshold: 0.5 });

    const snapshot = source.export();
    dest = new Audrey({
      dataDir: IMPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await dest.import(snapshot);
    const stats = dest.introspect();
    expect(stats.semantic).toBeGreaterThanOrEqual(1);
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/import.test.js`
Expected: FAIL — `audrey.import is not a function`

**Step 3: Write implementation**

Create `src/import.js`:

```js
export async function importMemories(db, embeddingProvider, snapshot) {
  const existingEpisodes = db.prepare('SELECT COUNT(*) as c FROM episodes').get().c;
  if (existingEpisodes > 0) {
    throw new Error('Cannot import into a database that is not empty');
  }

  const insertEpisode = db.prepare(`
    INSERT INTO episodes (id, content, source, source_reliability, salience, tags,
      causal_trigger, causal_consequence, created_at, supersedes, superseded_by, consolidated)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  `);

  const insertVecEpisode = db.prepare(
    'INSERT INTO vec_episodes(id, embedding, source, consolidated) VALUES (?, ?, ?, ?)'
  );

  const insertSemantic = db.prepare(`
    INSERT INTO semantics (id, content, state, conditions, evidence_episode_ids,
      evidence_count, supporting_count, contradicting_count, source_type_diversity,
      consolidation_checkpoint, created_at, last_reinforced_at, retrieval_count, challenge_count)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  `);

  const insertVecSemantic = db.prepare(
    'INSERT INTO vec_semantics(id, embedding, state) VALUES (?, ?, ?)'
  );

  const insertProcedure = db.prepare(`
    INSERT INTO procedures (id, content, state, trigger_conditions, evidence_episode_ids,
      success_count, failure_count, created_at, last_reinforced_at, retrieval_count)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  `);

  const insertVecProcedure = db.prepare(
    'INSERT INTO vec_procedures(id, embedding, state) VALUES (?, ?, ?)'
  );

  const insertCausalLink = db.prepare(`
    INSERT INTO causal_links (id, cause_id, effect_id, link_type, mechanism, confidence, evidence_count, created_at)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?)
  `);

  const insertContradiction = db.prepare(`
    INSERT INTO contradictions (id, claim_a_id, claim_a_type, claim_b_id, claim_b_type,
      state, resolution, resolved_at, reopened_at, reopen_evidence_id, created_at)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)
  `);

  const insertConsolidationRun = db.prepare(`
    INSERT INTO consolidation_runs (id, input_episode_ids, output_memory_ids, started_at, completed_at, status)
    VALUES (?, ?, ?, ?, ?, ?)
  `);

  // Import episodes with fresh embeddings
  for (const ep of snapshot.episodes) {
    const tags = ep.tags ? JSON.stringify(ep.tags) : null;
    insertEpisode.run(
      ep.id, ep.content, ep.source, ep.source_reliability, ep.salience ?? 0.5,
      tags, ep.causal_trigger ?? null, ep.causal_consequence ?? null,
      ep.created_at, ep.supersedes ?? null, ep.superseded_by ?? null, ep.consolidated ?? 0,
    );

    const vector = await embeddingProvider.embed(ep.content);
    const buffer = embeddingProvider.vectorToBuffer(vector);
    insertVecEpisode.run(ep.id, buffer, ep.source, BigInt(ep.consolidated ?? 0));
  }

  // Import semantics with fresh embeddings
  for (const sem of (snapshot.semantics || [])) {
    insertSemantic.run(
      sem.id, sem.content, sem.state, sem.conditions ?? null,
      JSON.stringify(sem.evidence_episode_ids || []),
      sem.evidence_count ?? 0, sem.supporting_count ?? 0, sem.contradicting_count ?? 0,
      sem.source_type_diversity ?? 0, sem.consolidation_checkpoint ?? null,
      sem.created_at, sem.last_reinforced_at ?? null, sem.retrieval_count ?? 0, sem.challenge_count ?? 0,
    );

    const vector = await embeddingProvider.embed(sem.content);
    const buffer = embeddingProvider.vectorToBuffer(vector);
    insertVecSemantic.run(sem.id, buffer, sem.state);
  }

  // Import procedures with fresh embeddings
  for (const proc of (snapshot.procedures || [])) {
    insertProcedure.run(
      proc.id, proc.content, proc.state, proc.trigger_conditions ?? null,
      JSON.stringify(proc.evidence_episode_ids || []),
      proc.success_count ?? 0, proc.failure_count ?? 0,
      proc.created_at, proc.last_reinforced_at ?? null, proc.retrieval_count ?? 0,
    );

    const vector = await embeddingProvider.embed(proc.content);
    const buffer = embeddingProvider.vectorToBuffer(vector);
    insertVecProcedure.run(proc.id, buffer, proc.state);
  }

  // Import causal links (no embeddings)
  for (const link of (snapshot.causalLinks || [])) {
    insertCausalLink.run(
      link.id, link.cause_id, link.effect_id, link.link_type ?? 'causal',
      link.mechanism ?? null, link.confidence ?? null, link.evidence_count ?? 1, link.created_at,
    );
  }

  // Import contradictions (no embeddings)
  for (const con of (snapshot.contradictions || [])) {
    insertContradiction.run(
      con.id, con.claim_a_id, con.claim_a_type, con.claim_b_id, con.claim_b_type,
      con.state, con.resolution ?? null, con.resolved_at ?? null,
      con.reopened_at ?? null, con.reopen_evidence_id ?? null, con.created_at,
    );
  }

  // Import consolidation run metadata (no embeddings)
  for (const run of (snapshot.consolidationRuns || [])) {
    insertConsolidationRun.run(
      run.id, JSON.stringify(run.input_episode_ids || []),
      JSON.stringify(run.output_memory_ids || []),
      run.started_at ?? null, run.completed_at ?? null, run.status,
    );
  }
}
```

Add to `src/audrey.js`:
```js
import { importMemories } from './import.js';
```

Add method:
```js
async import(snapshot) {
  return importMemories(this.db, this.embeddingProvider, snapshot);
}
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/import.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/import.js src/audrey.js tests/import.test.js
git commit -m "feat: add memory import with re-embedding"
```

---

### Task 6: MCP tools for export/import

Expose `memory_export` and `memory_import` as MCP tools.

**Files:**
- Modify: `mcp-server/index.js` (add 2 tools)
- Modify: `tests/mcp-server.test.js` (add integration tests)

**Step 1: Write the failing test**

Add to `tests/mcp-server.test.js`:

```js
describe('MCP tool: memory_export + memory_import', () => {
  let audrey;
  const EXPORT_DIR = './test-mcp-export';
  const IMPORT_DIR = './test-mcp-import';

  beforeEach(async () => {
    if (existsSync(EXPORT_DIR)) rmSync(EXPORT_DIR, { recursive: true });
    if (existsSync(IMPORT_DIR)) rmSync(IMPORT_DIR, { recursive: true });
    audrey = new Audrey({
      dataDir: EXPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await audrey.encode({ content: 'MCP export test', source: 'told-by-user' });
  });

  afterEach(() => {
    audrey?.close();
    if (existsSync(EXPORT_DIR)) rmSync(EXPORT_DIR, { recursive: true });
    if (existsSync(IMPORT_DIR)) rmSync(IMPORT_DIR, { recursive: true });
  });

  it('round-trips through export and import', async () => {
    const snapshot = audrey.export();
    expect(snapshot.episodes.length).toBe(1);

    const dest = new Audrey({
      dataDir: IMPORT_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
    await dest.import(snapshot);
    const stats = dest.introspect();
    expect(stats.episodic).toBe(1);
    dest.close();
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/mcp-server.test.js`
Expected: Pass (this test uses SDK methods, not MCP protocol directly). The real verification is that the tool definitions in index.js are correct.

**Step 3: Add MCP tool definitions**

In `mcp-server/index.js`, add after the `memory_resolve_truth` tool:

```js
server.tool(
  'memory_export',
  {},
  async () => {
    try {
      const snapshot = audrey.export();
      return toolResult(snapshot);
    } catch (err) {
      return toolError(err);
    }
  },
);

server.tool(
  'memory_import',
  {
    snapshot: z.object({
      version: z.string(),
      episodes: z.array(z.any()),
      semantics: z.array(z.any()).optional(),
      procedures: z.array(z.any()).optional(),
      causalLinks: z.array(z.any()).optional(),
      contradictions: z.array(z.any()).optional(),
      consolidationRuns: z.array(z.any()).optional(),
      config: z.record(z.string()).optional(),
    }).passthrough().describe('A snapshot from memory_export'),
  },
  async ({ snapshot }) => {
    try {
      await audrey.import(snapshot);
      const stats = audrey.introspect();
      return toolResult({ imported: true, stats });
    } catch (err) {
      return toolError(err);
    }
  },
);
```

Update the install message from "5 tools" to "7 tools" and add the two new descriptions.

**Step 4: Run tests**

Run: `npx vitest run tests/mcp-server.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add mcp-server/index.js tests/mcp-server.test.js
git commit -m "feat: add memory_export and memory_import MCP tools"
```

---

### Task 7: Auto-consolidation scheduling

Add `audrey.startAutoConsolidate(intervalMs)` and `audrey.stopAutoConsolidate()` that run consolidation on a timer. The Audrey class stores the interval handle and cleans it up on `close()`.

**Files:**
- Modify: `src/audrey.js` (add start/stop methods, modify close)
- Test: `tests/auto-consolidate.test.js`

**Step 1: Write the failing test**

Create `tests/auto-consolidate.test.js`:

```js
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { Audrey } from '../src/index.js';
import { existsSync, rmSync } from 'node:fs';

const TEST_DIR = './test-auto-consolidate';

describe('auto-consolidation', () => {
  let audrey;

  beforeEach(() => {
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
    audrey = new Audrey({
      dataDir: TEST_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
  });

  afterEach(() => {
    audrey.close();
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
  });

  it('runs consolidation on interval', async () => {
    vi.useFakeTimers();
    const consolidateSpy = vi.spyOn(audrey, 'consolidate');
    audrey.startAutoConsolidate(1000);

    await vi.advanceTimersByTimeAsync(3500);

    expect(consolidateSpy).toHaveBeenCalledTimes(3);
    audrey.stopAutoConsolidate();
    vi.useRealTimers();
  });

  it('stopAutoConsolidate stops the interval', async () => {
    vi.useFakeTimers();
    const consolidateSpy = vi.spyOn(audrey, 'consolidate');
    audrey.startAutoConsolidate(1000);

    await vi.advanceTimersByTimeAsync(1500);
    audrey.stopAutoConsolidate();
    await vi.advanceTimersByTimeAsync(3000);

    expect(consolidateSpy).toHaveBeenCalledTimes(1);
    vi.useRealTimers();
  });

  it('close() stops auto-consolidation', async () => {
    vi.useFakeTimers();
    const consolidateSpy = vi.spyOn(audrey, 'consolidate');
    audrey.startAutoConsolidate(1000);

    await vi.advanceTimersByTimeAsync(1500);
    audrey.close();
    await vi.advanceTimersByTimeAsync(3000);

    expect(consolidateSpy).toHaveBeenCalledTimes(1);
    vi.useRealTimers();
  });

  it('emits consolidation events from auto-consolidate', async () => {
    vi.useFakeTimers();
    const events = [];
    audrey.on('consolidation', (e) => events.push(e));
    audrey.startAutoConsolidate(1000);

    await vi.advanceTimersByTimeAsync(1500);

    expect(events.length).toBe(1);
    audrey.stopAutoConsolidate();
    vi.useRealTimers();
  });

  it('throws if interval is too small', () => {
    expect(() => audrey.startAutoConsolidate(500)).toThrow();
  });

  it('throws if already running', () => {
    audrey.startAutoConsolidate(1000);
    expect(() => audrey.startAutoConsolidate(1000)).toThrow('already running');
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/auto-consolidate.test.js`
Expected: FAIL — `audrey.startAutoConsolidate is not a function`

**Step 3: Write implementation**

In `src/audrey.js`, add to constructor:
```js
this._autoConsolidateTimer = null;
```

Add methods:
```js
startAutoConsolidate(intervalMs, options = {}) {
  if (intervalMs < 1000) {
    throw new Error('Auto-consolidation interval must be at least 1000ms');
  }
  if (this._autoConsolidateTimer) {
    throw new Error('Auto-consolidation is already running');
  }
  this._autoConsolidateTimer = setInterval(() => {
    this.consolidate(options).catch(err => this.emit('error', err));
  }, intervalMs);
}

stopAutoConsolidate() {
  if (this._autoConsolidateTimer) {
    clearInterval(this._autoConsolidateTimer);
    this._autoConsolidateTimer = null;
  }
}
```

Modify `close()`:
```js
close() {
  this.stopAutoConsolidate();
  closeDatabase(this.db);
}
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/auto-consolidate.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/audrey.js tests/auto-consolidate.test.js
git commit -m "feat: add auto-consolidation scheduling"
```

---

### Task 8: Adaptive consolidation — track cluster quality

Store per-run cluster quality metrics so Audrey can learn what cluster sizes and thresholds work best. Add a `consolidation_metrics` table that logs each run's params and results.

**Files:**
- Modify: `src/db.js` (add consolidation_metrics table)
- Modify: `src/consolidate.js` (log metrics after each run)
- Test: `tests/consolidate.test.js`

**Step 1: Write the failing test**

Add to `tests/consolidate.test.js` (read the file first to find the right describe block):

```js
it('logs consolidation metrics per run', async () => {
  // Encode enough identical episodes to form a cluster
  await encodeEpisode(db, embedding, { content: 'metric test', source: 'told-by-user' });
  await encodeEpisode(db, embedding, { content: 'metric test', source: 'direct-observation' });
  await encodeEpisode(db, embedding, { content: 'metric test', source: 'tool-result' });

  const result = await runConsolidation(db, embedding, {
    minClusterSize: 3,
    similarityThreshold: 0.99,
  });

  const metrics = db.prepare('SELECT * FROM consolidation_metrics WHERE run_id = ?').all(result.runId);
  expect(metrics.length).toBeGreaterThanOrEqual(1);
  expect(metrics[0]).toHaveProperty('min_cluster_size');
  expect(metrics[0]).toHaveProperty('similarity_threshold');
  expect(metrics[0]).toHaveProperty('clusters_found');
  expect(metrics[0]).toHaveProperty('principles_extracted');
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/consolidate.test.js`
Expected: FAIL — `no such table: consolidation_metrics`

**Step 3: Write implementation**

In `src/db.js`, add to the SCHEMA string (before the CREATE INDEX statements):

```sql
CREATE TABLE IF NOT EXISTS consolidation_metrics (
  id TEXT PRIMARY KEY,
  run_id TEXT NOT NULL,
  min_cluster_size INTEGER NOT NULL,
  similarity_threshold REAL NOT NULL,
  episodes_evaluated INTEGER NOT NULL,
  clusters_found INTEGER NOT NULL,
  principles_extracted INTEGER NOT NULL,
  created_at TEXT NOT NULL,
  FOREIGN KEY (run_id) REFERENCES consolidation_runs(id)
);
```

In `src/consolidate.js`, after `promoteAll()` and before the `return`, add:

```js
import { generateId } from './ulid.js';
// (already imported above)

db.prepare(`
  INSERT INTO consolidation_metrics (id, run_id, min_cluster_size, similarity_threshold,
    episodes_evaluated, clusters_found, principles_extracted, created_at)
  VALUES (?, ?, ?, ?, ?, ?, ?, ?)
`).run(
  generateId(), runId, minClusterSize, similarityThreshold,
  episodesEvaluated, clusters.length, principlesExtracted, new Date().toISOString(),
);
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/consolidate.test.js`
Expected: PASS

**Step 5: Run full suite**

Run: `npx vitest run`
Expected: All tests pass

**Step 6: Commit**

```bash
git add src/db.js src/consolidate.js tests/consolidate.test.js
git commit -m "feat: track consolidation metrics per run"
```

---

### Task 9: Adaptive consolidation — suggest optimal params

Add `audrey.suggestConsolidationParams()` that analyzes historical consolidation metrics and recommends `minClusterSize` and `similarityThreshold` based on what has worked best (highest principles-per-cluster ratio).

**Files:**
- Create: `src/adaptive.js`
- Modify: `src/audrey.js` (add method)
- Test: `tests/adaptive.test.js`

**Step 1: Write the failing test**

Create `tests/adaptive.test.js`:

```js
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { Audrey } from '../src/index.js';
import { existsSync, rmSync } from 'node:fs';

const TEST_DIR = './test-adaptive';

describe('suggestConsolidationParams', () => {
  let audrey;

  beforeEach(() => {
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
    audrey = new Audrey({
      dataDir: TEST_DIR,
      embedding: { provider: 'mock', dimensions: 8 },
    });
  });

  afterEach(() => {
    audrey.close();
    if (existsSync(TEST_DIR)) rmSync(TEST_DIR, { recursive: true });
  });

  it('returns defaults when no history exists', () => {
    const params = audrey.suggestConsolidationParams();
    expect(params).toHaveProperty('minClusterSize');
    expect(params).toHaveProperty('similarityThreshold');
    expect(params).toHaveProperty('confidence');
    expect(params.confidence).toBe('no_data');
  });

  it('suggests params based on historical success', async () => {
    // Simulate multiple consolidation runs with different params
    // Run 1: minClusterSize=3, threshold=0.85, produced 2 principles from 1 cluster
    audrey.db.prepare(`
      INSERT INTO consolidation_metrics (id, run_id, min_cluster_size, similarity_threshold,
        episodes_evaluated, clusters_found, principles_extracted, created_at)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    `).run('m1', 'r1', 3, 0.85, 10, 1, 2, new Date().toISOString());

    // Run 2: minClusterSize=2, threshold=0.70, produced 5 principles from 3 clusters
    audrey.db.prepare(`
      INSERT INTO consolidation_metrics (id, run_id, min_cluster_size, similarity_threshold,
        episodes_evaluated, clusters_found, principles_extracted, created_at)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    `).run('m2', 'r2', 2, 0.70, 10, 3, 5, new Date().toISOString());

    // Run 3: same as run 2 params, also good
    audrey.db.prepare(`
      INSERT INTO consolidation_metrics (id, run_id, min_cluster_size, similarity_threshold,
        episodes_evaluated, clusters_found, principles_extracted, created_at)
      VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    `).run('m3', 'r3', 2, 0.70, 15, 4, 6, new Date().toISOString());

    const params = audrey.suggestConsolidationParams();
    expect(params.confidence).not.toBe('no_data');
    expect(params.minClusterSize).toBe(2);
    expect(params.similarityThreshold).toBeCloseTo(0.70, 1);
  });
});
```

**Step 2: Run test to verify it fails**

Run: `npx vitest run tests/adaptive.test.js`
Expected: FAIL — `audrey.suggestConsolidationParams is not a function`

**Step 3: Write implementation**

Create `src/adaptive.js`:

```js
export function suggestConsolidationParams(db) {
  const runs = db.prepare(`
    SELECT min_cluster_size, similarity_threshold, clusters_found, principles_extracted, episodes_evaluated
    FROM consolidation_metrics
    ORDER BY created_at DESC
    LIMIT 20
  `).all();

  if (runs.length === 0) {
    return {
      minClusterSize: 3,
      similarityThreshold: 0.85,
      confidence: 'no_data',
    };
  }

  // Score each run: principles_extracted / episodes_evaluated (yield rate)
  // Group by param combination, average the yield rates
  const paramScores = new Map();
  for (const run of runs) {
    if (run.episodes_evaluated === 0) continue;
    const key = `${run.min_cluster_size}:${run.similarity_threshold}`;
    if (!paramScores.has(key)) {
      paramScores.set(key, {
        minClusterSize: run.min_cluster_size,
        similarityThreshold: run.similarity_threshold,
        yields: [],
      });
    }
    paramScores.get(key).yields.push(run.principles_extracted / run.episodes_evaluated);
  }

  let bestKey = null;
  let bestAvgYield = -1;
  for (const [key, data] of paramScores) {
    const avg = data.yields.reduce((a, b) => a + b, 0) / data.yields.length;
    if (avg > bestAvgYield) {
      bestAvgYield = avg;
      bestKey = key;
    }
  }

  if (!bestKey) {
    return { minClusterSize: 3, similarityThreshold: 0.85, confidence: 'no_data' };
  }

  const best = paramScores.get(bestKey);
  const confidence = runs.length >= 5 ? 'high' : runs.length >= 2 ? 'medium' : 'low';

  return {
    minClusterSize: best.minClusterSize,
    similarityThreshold: best.similarityThreshold,
    confidence,
  };
}
```

Add to `src/audrey.js`:
```js
import { suggestConsolidationParams } from './adaptive.js';
```

Add method:
```js
suggestConsolidationParams() {
  return suggestConsolidationParams(this.db);
}
```

**Step 4: Run test to verify it passes**

Run: `npx vitest run tests/adaptive.test.js`
Expected: PASS

**Step 5: Commit**

```bash
git add src/adaptive.js src/audrey.js tests/adaptive.test.js
git commit -m "feat: adaptive consolidation param suggestions"
```

---

### Task 10: Export barrel updates and index.js

Ensure all new exports are available from the barrel.

**Files:**
- Modify: `src/index.js`

**Step 1: Update exports**

Add to `src/index.js`:

```js
export { exportMemories } from './export.js';
export { importMemories } from './import.js';
export { suggestConsolidationParams } from './adaptive.js';
```

**Step 2: Run full test suite**

Run: `npx vitest run`
Expected: All tests pass

**Step 3: Commit**

```bash
git add src/index.js
git commit -m "feat: export new modules from barrel"
```

---

### Task 11: Version bump, README, and final verification

Bump to v0.5.0. Update README features list. Run full test suite.

**Files:**
- Modify: `package.json` (version)
- Modify: `mcp-server/config.js` (VERSION)
- Modify: `README.md` (features, test count, roadmap)

**Step 1: Bump version**

In `package.json`: `"version": "0.5.0"`
In `mcp-server/config.js`: `export const VERSION = '0.5.0';`

**Step 2: Update README**

Update the roadmap section to mark v0.5.0 features as complete:
- Configurable decay & weights
- Memory export/import
- Auto-consolidation scheduling
- Adaptive consolidation

Update the test count to match the final number.

Update the install message in `mcp-server/index.js` from 5 tools to 7 tools (if not already done in Task 6).

**Step 3: Run full test suite**

Run: `npx vitest run`
Expected: All tests pass

**Step 4: Commit**

```bash
git add package.json mcp-server/config.js README.md mcp-server/index.js
git commit -m "chore: bump version to 0.5.0, update README"
```
